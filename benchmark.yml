config:
  target: 'http://localhost:3000'
  phases:
    - duration: 30
      arrivalRate: 10
    - duration: 30
      arrivalRate: 50
    - duration: 60
      arrivalRate: 100
  defaults:
    headers:
      Content-Type: 'application/json'
scenarios:
  - name: 'Send FT'
    requests:
      - post:
          url: '/send-ft'
          json:
            receiverId: 'posma-badge.testnet'
            amount: '100000'
            memo: 'Artillery Test'